#include <iostream>
using namespace std; // Ricorda sempre di chiudere una espressione con semicolon! (;)

/*  dichiarazione della funzione (int, bool, void, float)
    int restituisce un valore intero, bool restituisce 0 o 1 ovvero vero o falso, void non restituisce alcun valore,
    mentre float restituisce un valore con decimale.
    
    Esemplificazione:

    int = 12, 23, 1, 3, -4 , -100, -10000
    float = 1.2301, 12.13, 100.32
    bool = 0 = false || 1 = true
    char = a, b ,c , d, e, f, g. h
    */

int main(){
    cout << "Hello, World!\n"; //  \n servira' a mandare a capo l'output della espressione, dando un look leggibile.

   // system("pause"); // puoi omettere, ma se sei su ambiente windows, puo' tornarti utile! Non chiude la finestra automaticamente.
}